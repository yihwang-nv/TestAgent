# ── Inference engine ──────────────────────────────────────────────────────────
# On this machine vLLM ships as 0.18.0+cu134 from https://pypi.nvidia.com.
# Install with: pip install vllm --extra-index-url https://pypi.nvidia.com
# nvidia-cutlass is required by vLLM's flash-attention on Blackwell GPUs.
# WARNING: Do NOT use "cutlass" (unrelated stats package). Must be "nvidia-cutlass".
vllm>=0.18.0
nvidia-cutlass
bitsandbytes>=0.48.1  # optional: only needed for --quant 8bit

# ── Anthropic SDK (Claude Code agent) ────────────────────────────────────────
anthropic>=0.52.0

# ── HTTP client (agent ↔ local server) ───────────────────────────────────────
httpx>=0.27.0

# ── Model download ────────────────────────────────────────────────────────────
huggingface_hub>=0.23.0

# ── Utilities ─────────────────────────────────────────────────────────────────
rich>=13.7.0
typer>=0.12.0
pyyaml>=6.0
prompt_toolkit>=3.0.0

# claude_agent.py --kb (vector RAG + PDF/DOCX + optional OCR; skip if unused)
chromadb>=0.5.0
sentence-transformers>=3.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
pytesseract>=0.3.10
Pillow>=10.0.0
